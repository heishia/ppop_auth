# Nixpacks 설정 - Next.js 앱용
[phases.setup]
nixPkgs = ["nodejs_22", "npm-10_x"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
# Next.js standalone 모드로 실행
cmd = "node .next/standalone/server.js"

