# Auth Server 환경 변수
# 이 파일을 .env로 복사한 후 실제 값으로 변경하세요
# cp env.example .env

# Database (Supabase)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres

# JWT 키 설정 (2가지 방법 중 선택)
# 방법 1: 파일 경로 (로컬 개발용)
JWT_PRIVATE_KEY_PATH=../keys/private.pem
JWT_PUBLIC_KEY_PATH=../keys/public.pem

# 방법 2: 환경변수에 키 직접 입력 (Railway 배포용)
# 줄바꿈은 \n으로 입력 (예: -----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----)
# JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
# JWT_PUBLIC_KEY=-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----

# 토큰 만료 시간
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# 서버 설정
AUTH_SERVER_PORT=3000
AUTH_SERVER_URL=http://localhost:3000

# CORS (쉼표로 구분)
CORS_ORIGINS=http://localhost:3001,http://localhost:3002

# 네이버 클라우드 SMS 설정 (선택사항 - 미설정시 Mock 모드)
NAVER_SMS_ACCESS_KEY=your-access-key
NAVER_SMS_SECRET_KEY=your-secret-key
NAVER_SMS_SERVICE_ID=your-service-id
NAVER_SMS_SENDER=01012345678

# 관리자 API 키 (Make/Zapier 자동화용)
# 안전한 랜덤 문자열 생성: openssl rand -hex 32
ADMIN_API_KEY=your-admin-api-key-here

# Groble 결제 연동 (Make/Zapier로 자동화)
# 1. Make.com에서 Gmail 트리거 설정
# 2. Groble 결제 알림 이메일 파싱
# 3. POST /api/admin/subscriptions/activate 호출

# 소셜 로그인 설정
# 프론트엔드 URL (콜백 후 리다이렉트용)
AUTH_CLIENT_URL=http://localhost:3001

# 카카오 (https://developers.kakao.com)
# REST API 키 -> KAKAO_CLIENT_ID
# 보안 -> Client Secret -> KAKAO_CLIENT_SECRET
KAKAO_CLIENT_ID=your-kakao-client-id
KAKAO_CLIENT_SECRET=your-kakao-client-secret

# 네이버 (https://developers.naver.com)
NAVER_CLIENT_ID=your-naver-client-id
NAVER_CLIENT_SECRET=your-naver-client-secret

# 구글 (https://console.cloud.google.com)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
